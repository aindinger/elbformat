		static boolean palindrom(String s) {
		String wort = s.toLowerCase();
		int n = wort.length();
		
		for(int i = 0; i < n/2; i++) {
			if(wort.charAt(i) != wort.charAt(n-i-1)){return false;}
	}
return true;
}
